# shakedown ランブックテンプレート

SKILL.md Step 7 で使う。`<>` はプレースホルダ。該当しないセクションは丸ごと削除する。再ベースライン節は再ベースライン時のみ残す。合意ゲートを通過した内容だけを流し込み、新しい事実をここで初出させない。

文体ルール: 括弧書きを使わない。補足は本文の文にするか削る。チェックリスト項目は最短の文で書く。

````markdown
# <チケットID> <機能名> shakedown ランブック

## 概要

| 項目 | 内容 |
|---|---|
| チケット | <チケットID または none> |
| 機能名 | <機能名> |
| 対象 diff | <base>...<head> |
| 基準 SHA | <SHA> |
| 機能タイプ | <カスタマーUI / 管理画面 / バックエンド / 非同期ジョブ / フラグ配下> |
| Feature Flag | <フラグ名と各環境の状態。なければ none> |
| 環境 | <dev / staging など> |
| 実装者 | <名前> |
| Blocking 実行者 | <名前> |
| 生成日 | <YYYY-MM-DD> |
| 実施ステータス | 未実施 |

[!] HEAD が基準 SHA から動いた場合、本ランブックは陳腐化している。/shakedown で再生成すること。

<Blocking 実行者が実装者本人の場合、その理由と、実装から時間を置いて実行する宣言をここに 1 行で書く。>

## なぜ作り直すのか

再ベースライン時のみ残す。

- 旧版: <旧ランブックへの相対リンク>
- 旧版の結果が流用できない理由: <1〜3 文>

| 観点 | 旧実装 | 現行実装 |
|---|---|---|
| <観点> | <旧QAが測定したもの> | <本QAの対象> |

## 前提条件と環境制約

各前提には根拠として file:line かインフラ定義のパスを添える。根拠のない前提を書かない。

- <前提> — <根拠パス>

## クリティカルパス

<分岐が 2 つ以上あるときだけ Mermaid flowchart LR。15 ノード以内。diff 接触ノードに classDef changed。分岐にシナリオ ID。線形フローなら番号付きステップリスト。>

### 検証責任の切り分け

| 観点 | 自動テストで固定済み | 手動QA必須 | どちらにも落ちていない |
|---|---|---|---|
| <観点> | <テストへの参照 / -> | <シナリオID / -> | <[!] 要対応 / -> |

「どちらにも落ちていない」列に印が付いた観点は、tdd-test-cases でのテスト追加を検討するか本ランブックのシナリオに昇格させる。放置しない。

## シナリオ一覧

| ID | タグ | シナリオ | 根拠 | anchor |
|---|---|---|---|---|
| S1 | Blocking | <名前> | <1行根拠> | <file:line / docセクション> |
| S2 | Should-verify | <名前> | <1行根拠> | <file:line / docセクション / [unanchored]> |

Blocking の anchor は file:line か doc セクションの実在するものだけを書く。`[unanchored]` は Should-verify 限定。

## Blocking チェックリスト

初見の実行者が質問なしで実行できる自己完結性を保つ。1 項目 1 シナリオ。期待結果は表示文言・遷移先・DB 行・ログ行を名指しし、「正しく動く」と書かない。pass/fail に落ちない気づきは問題台帳に種別「懸念」で記録する。

### S<n>: <シナリオ名>

- 実行者: <名前>
- 前提: <必要なデータと環境状態。準備コマンドがあれば貼る>
- 手順:
  1. <操作>
  2. <操作>
- 期待結果: <観測可能な状態>
- 証跡: <出力・スクリーンショット・クエリ結果を貼る欄>
- 結果: [ ] pass / [ ] fail

<非同期ジョブのスケール検証がある場合、段階ごとの記録表を設ける:>

| 項目 | 数十件 | 数千件 | 本番相当 |
|---|---|---|---|
| 所要時間 / throughput | | | |
| 再試行・手動介入回数 | | | |
| 合格基準: <DB で観測可能な数値> | | | |

<進捗確認用に、貼るだけで使える SQL やコマンドをここに同梱する。>

## Should-verify

実施者が確認してチェックする。証跡不要。

- [ ] S<n> <1 行確認>

## 検証しないもの

ここに載せたものは検証されない。承認済みのスコープ決定として記録する。

| ID | 内容 | 理由 |
|---|---|---|
| S<n> | <内容> | <スコープ外 / 自動テスト済みでテストへの参照 / コストがリスクを上回る> |

## 問題台帳

| # | 種別 | 内容 | Issue | 対応状況 |
|---|---|---|---|---|
| F1 | <バグ / 懸念 / 質問> | <再現手順と根拠 file:line を含める> | <#番号> | <未対応 / 対応中 / 対応済> |

[!] 問題を発見したら、その場で create-github-issues スキルにより機能 Epic の Sub-issue として起票し、Issue 番号をこの列に記録する。自由記述のまま放置しない。Issue 列が空欄の行が残っている限り、確認完了にはできない。

## 確認完了

完了基準。すべて満たすまで完了にできない。

- [ ] Blocking の fail が 0 件。fail が残る場合はリリース可否の判断と条件を下に注記する
- [ ] 問題台帳に Issue 番号が空欄の行が 0 件
- [ ] Blocking 実行者が記名済み

| 項目 | 内容 |
|---|---|
| 実施者 | |
| 実施日 | |
| 判定 | go / no-go |
| 条件 | <例: F3 修正が本番 ON の前に必須。なければ - > |
````
